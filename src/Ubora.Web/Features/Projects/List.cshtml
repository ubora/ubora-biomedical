@model Ubora.Web.Features.Projects.ListViewModel

@{
    ViewData["Title"] = "All";
}

<h2>All</h2>

<ul>
    @foreach (var project in Model.Projects)
    {
        <li>
            <a asp-controller="ProjectManagement"
               asp-action="Index"
               asp-route-id="@project.Id">
                @project.Title
            </a>
        </li>
    }
</ul>