<!DOCTYPE html>
<html lang="en">
<head>
    @RenderSection("Head", required: false)
    @{
        Html.RenderPartial("_LayoutHead");
    }
    <link href="//cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
</head>

<body>
    @{
        Html.RenderPartial("_Header");
    }

    <main>
        <div class="container-fluid px-0">
            <div class="row no-gutters">
                <div class="col-lg-2 py-lg-3 px-lg-3 px-sm-0 @(IsSectionDefined("left") ? "border-right" : "") h-min-lg-50">
                    @{
                        if (IsSectionDefined("left"))
                        {
                            @RenderSection("Left", required: false)
                        }
                    }
                </div>
                <div class="col-lg-8 order-lg-1 order-2 py-lg-3 px-xl-7 px-3">
                    @RenderBody()
                </div>
                <div class="col-lg-2 order-lg-2 order-1 py-lg-3 px-3 @(IsSectionDefined("Right") ? "border-left" : "")">
                    @RenderSection("Right", required: false)
                </div>
            </div>
        </div>
    </main>

    @{
        Html.RenderPartial("_Footer");
        Html.RenderPartial("_LayoutBody");
    }
    <script src="//cdn.quilljs.com/1.3.6/quill.js"></script>
    <script src="~/dist/resources.bundle.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>