{
    "commands": {
        "restore": {
            "description": "Restore .NET",
            "command": ["dotnet restore"]
        },
        "run": {
            "description": "Run .NET",
            "command": ["dotnet run"]
        },
        "db": {
            "description": "Restore Docker DB",
            "command": ["docker run -p 5400:5432 -e 'POSTGRES_PASSWORD=ubora' -e 'POSTGRES_DB=ubora' postgres"]
        }
    }
}
